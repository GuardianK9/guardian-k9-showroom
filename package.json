{
  "name": "guardian-k9-showroom",
  "private": true,
  "scripts": {
    "build": "esbuild main.js --bundle --format=iife --platform=browser --target=es2020 --outfile=bundle.js --minify"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}